<style>
  /* ==========================================================
     REVENUE CARD
     ========================================================== */

  .financial-card.revenue-card {
    position: relative;
    overflow: hidden;

    width: 100%;
    max-width: 900px;
    margin: 24px auto;
    padding: 0;

    border-radius: 24px;

    background:
      linear-gradient(
        145deg,
        #ffffff 0%,
        #f8fafc 55%,
        #f0fdf4 100%
      );

    border: 1px solid rgba(15, 23, 42, 0.08);

    box-shadow:
      0 20px 45px rgba(15, 23, 42, 0.08),
      0 5px 15px rgba(15, 23, 42, 0.04);

    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }

  .financial-card.revenue-card:hover {
    transform: translateY(-2px);

    box-shadow:
      0 25px 55px rgba(15, 23, 42, 0.10),
      0 7px 18px rgba(15, 23, 42, 0.05);
  }


  /* ==========================================================
     DECORATIVE GLOW
     ========================================================== */

  .revenue-card .financial-card-glow {
    position: absolute;

    top: -110px;
    right: -80px;

    width: 280px;
    height: 280px;

    border-radius: 50%;

    background: rgba(16, 185, 129, 0.12);

    filter: blur(8px);

    pointer-events: none;
  }

  .revenue-card .financial-card-glow::after {
    content: "";

    position: absolute;

    left: -190px;
    top: 200px;

    width: 190px;
    height: 190px;

    border-radius: 50%;

    background: rgba(59, 130, 246, 0.05);
  }


  /* ==========================================================
     HEADER
     ========================================================== */

  .revenue-card .financial-section-header {
    position: relative;
    z-index: 1;

    display: flex;
    align-items: center;

    gap: 18px;

    padding: 28px 30px 24px;
  }


  /* ==========================================================
     REVENUE ICON
     ========================================================== */

  .revenue-card .financial-section-icon {
    position: relative;

    flex: 0 0 auto;

    width: 64px;
    height: 64px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 18px;

    background:
      linear-gradient(
        145deg,
        #10b981,
        #047857
      );

    color: #ffffff;

    box-shadow:
      0 10px 22px rgba(5, 150, 105, 0.25);
  }

  .revenue-card .financial-section-icon::before {
    content: "";

    position: absolute;
    inset: 5px;

    border-radius: 14px;

    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .revenue-card .revenue-icon span {
    position: relative;
    z-index: 1;

    font-size: 30px;
    font-weight: 800;
    line-height: 1;
  }


  /* ==========================================================
     HEADER TEXT
     ========================================================== */

  .revenue-card .financial-section-title {
    min-width: 0;
  }

  .revenue-card .financial-eyebrow {
    display: block;

    margin-bottom: 5px;

    color: #059669;

    font-size: 11px;
    font-weight: 800;

    letter-spacing: 1.5px;
    text-transform: uppercase;
  }

  .revenue-card .financial-section-title h2 {
    margin: 0;

    color: #0f172a;

    font-size: clamp(21px, 4vw, 28px);
    font-weight: 800;

    line-height: 1.2;
  }

  .revenue-card .financial-section-title p {
    margin: 7px 0 0;

    color: #64748b;

    font-size: 14px;
    line-height: 1.5;
  }

  .revenue-card .financial-section-title strong {
    color: #334155;
    font-weight: 700;
  }


  /* ==========================================================
     DIVIDER
     ========================================================== */

  .revenue-card .financial-welcome-divider {
    position: relative;

    height: 1px;

    margin: 0 30px;

    background:
      linear-gradient(
        to right,
        rgba(15, 23, 42, 0.08),
        rgba(15, 23, 42, 0.03),
        transparent
      );
  }


  /* ==========================================================
     FORM
     ========================================================== */

  .revenue-card .financial-form {
    position: relative;
    z-index: 1;

    padding: 28px 30px 30px;
  }


  /* ==========================================================
     FORM GROUP
     ========================================================== */

  .revenue-card .form-group {
    margin-bottom: 23px;
  }

  .revenue-card .form-group:last-of-type {
    margin-bottom: 0;
  }

  .revenue-card .form-group label {
    display: block;

    margin-bottom: 8px;

    color: #1e293b;

    font-size: 13px;
    font-weight: 750;
  }


  /* ==========================================================
     INPUTS
     ========================================================== */

  .revenue-card input,
  .revenue-card textarea {
    width: 100%;

    box-sizing: border-box;

    border: 1px solid #dbe2ea;

    border-radius: 13px;

    background: rgba(255, 255, 255, 0.92);

    color: #0f172a;

    font-family: inherit;
    font-size: 15px;

    outline: none;

    transition:
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      background 0.2s ease;
  }

  .revenue-card input {
    min-height: 50px;

    padding: 0 15px;
  }

  .revenue-card textarea {
    display: block;

    min-height: 112px;

    padding: 14px 15px;

    resize: vertical;

    line-height: 1.55;
  }

  .revenue-card input::placeholder,
  .revenue-card textarea::placeholder {
    color: #94a3b8;
  }

  .revenue-card input:focus,
  .revenue-card textarea:focus {
    border-color: #10b981;

    background: #ffffff;

    box-shadow:
      0 0 0 4px rgba(16, 185, 129, 0.10);
  }


  /* ==========================================================
     AMOUNT INPUT
     ========================================================== */

  .revenue-card .financial-input-wrapper {
    position: relative;
  }

  .revenue-card .financial-input-prefix {
    position: absolute;

    left: 15px;
    top: 50%;

    transform: translateY(-50%);

    color: #047857;

    font-size: 13px;
    font-weight: 800;

    pointer-events: none;

    z-index: 2;
  }

  .revenue-card .financial-input-wrapper input {
    padding-left: 55px;
  }


  /* ==========================================================
     FIELD HINT
     ========================================================== */

  .revenue-card .financial-field-hint {
    display: block;

    margin-top: 7px;

    color: #64748b;

    font-size: 12px;

    line-height: 1.5;
  }


  /* ==========================================================
     FORM ACTION
     ========================================================== */

  .revenue-card .financial-form-action {
    display: flex;
    justify-content: flex-end;

    margin-top: 28px;

    padding-top: 22px;

    border-top: 1px solid rgba(15, 23, 42, 0.06);
  }


  /* ==========================================================
     BUTTON
     ========================================================== */

  .revenue-card .financial-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    gap: 10px;

    min-height: 50px;

    padding: 0 20px;

    border: 0;
    border-radius: 13px;

    background:
      linear-gradient(
        135deg,
        #10b981,
        #047857
      );

    color: #ffffff;

    font-family: inherit;
    font-size: 14px;
    font-weight: 800;

    cursor: pointer;

    box-shadow:
      0 9px 18px rgba(5, 150, 105, 0.20);

    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      filter 0.2s ease;
  }

  .revenue-card .financial-btn:hover {
    transform: translateY(-1px);

    filter: brightness(1.03);

    box-shadow:
      0 12px 24px rgba(5, 150, 105, 0.25);
  }

  .revenue-card .financial-btn:active {
    transform: translateY(0);

    box-shadow:
      0 5px 12px rgba(5, 150, 105, 0.18);
  }

  .revenue-card .financial-btn:focus-visible {
    outline: none;

    box-shadow:
      0 0 0 4px rgba(16, 185, 129, 0.15),
      0 9px 18px rgba(5, 150, 105, 0.20);
  }


  /* ==========================================================
     BUTTON ICON
     ========================================================== */

  .revenue-card .financial-btn-icon {
    width: 27px;
    height: 27px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    border-radius: 8px;

    background: rgba(255, 255, 255, 0.17);

    font-size: 14px;
    font-weight: 900;
  }


  /* ==========================================================
     MOBILE
     ========================================================== */

  @media (max-width: 600px) {

    .financial-card.revenue-card {
      margin: 16px auto;

      border-radius: 20px;
    }

    .revenue-card .financial-section-header {
      gap: 14px;

      padding: 22px 20px 20px;
    }

    .revenue-card .financial-section-icon {
      width: 54px;
      height: 54px;

      border-radius: 15px;
    }

    .revenue-card .financial-section-icon::before {
      border-radius: 11px;
    }

    .revenue-card .revenue-icon span {
      font-size: 25px;
    }

    .revenue-card .financial-eyebrow {
      font-size: 9px;
      letter-spacing: 1.2px;
    }

    .revenue-card .financial-section-title h2 {
      font-size: 20px;
    }

    .revenue-card .financial-section-title p {
      font-size: 13px;
    }

    .revenue-card .financial-welcome-divider {
      margin: 0 20px;
    }

    .revenue-card .financial-form {
      padding: 23px 20px 22px;
    }

    .revenue-card .financial-form-action {
      margin-top: 24px;
      padding-top: 20px;
    }

    .revenue-card .financial-btn {
      width: 100%;
    }
  }


  /* ==========================================================
     VERY SMALL SCREENS
     ========================================================== */

  @media (max-width: 380px) {

    .revenue-card .financial-section-icon {
      width: 48px;
      height: 48px;
    }

    .revenue-card .revenue-icon span {
      font-size: 22px;
    }

    .revenue-card .financial-section-title h2 {
      font-size: 18px;
    }

    .revenue-card .financial-section-title p {
      font-size: 12px;
    }

    .revenue-card .form-group label {
      font-size: 12px;
    }

    .revenue-card input,
    .revenue-card textarea {
      font-size: 14px;
    }
  }
</style>


<div class="financial-card revenue-card">

  <div class="financial-card-glow"></div>


  <!-- ========================================================
       HEADER
       ======================================================== -->

  <div class="financial-section-header">

    <div
      class="financial-section-icon revenue-icon"
      aria-hidden="true"
    >
      <span>+</span>
    </div>


    <div class="financial-section-title">

      <span class="financial-eyebrow">
        Revenue
      </span>

      <h2>
        Record Revenue
      </h2>

      <p>
        Add income generated by
        <strong><%= dairy.name %></strong>.
      </p>

    </div>

  </div>


  <div class="financial-welcome-divider"></div>


  <!-- ========================================================
       FORM
       ======================================================== -->

  <form
    action="/financials/revenue"
    method="POST"
    class="financial-form"
  >

    <input
      type="hidden"
      name="dairy"
      value="<%= dairy._id %>"
    >


    <!-- ======================================================
         AMOUNT
         ====================================================== -->

    <div class="form-group">

      <label for="revenueAmount">
        Revenue Amount
      </label>

      <div class="financial-input-wrapper">

        <span
          class="financial-input-prefix"
          aria-hidden="true"
        >
          KSh
        </span>

        <input
          type="number"
          id="revenueAmount"
          name="amount"
          placeholder="0.00"
          min="0"
          step="0.01"
          inputmode="decimal"
          autocomplete="off"
          required
        >

      </div>

      <small class="financial-field-hint">
        Enter the amount received or generated.
      </small>

    </div>


    <!-- ======================================================
         DESCRIPTION
         ====================================================== -->

    <div class="form-group">

      <label for="revenueDescription">
        Description
      </label>

      <textarea
        id="revenueDescription"
        name="description"
        placeholder="e.g. Milk sales, animal sale, manure sales..."
        rows="4"
        required
      ></textarea>

      <small class="financial-field-hint">
        Briefly explain the source of this revenue.
      </small>

    </div>


    <!-- ======================================================
         DATE
         ====================================================== -->

    <div class="form-group">

      <label for="revenueDate">
        Revenue Date
      </label>

      <input
        type="date"
        id="revenueDate"
        name="date"
        required
      >

      <small class="financial-field-hint">
        Select the date the revenue was generated.
      </small>

    </div>


    <!-- ======================================================
         ACTION
         ====================================================== -->

    <div class="financial-form-action">

      <button
        type="submit"
        class="financial-btn revenue-btn"
      >

        <span
          class="financial-btn-icon"
          aria-hidden="true"
        >
          ✓
        </span>

        <span>
          Record Revenue
        </span>

      </button>

    </div>

  </form>

</div>