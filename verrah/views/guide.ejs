<% if (
    user &&
    user.role === "staff"
) { %>

    <%- include("home-partials/staff/hero") %>

    <%- include("home-partials/staff/delivery") %>

    <%- include("home-partials/staff/sales") %>


<% } else if (
    user &&
    user.role === "admin"
) { %>

    <%- include("home-partials/admin/cards") %>

<% } %>