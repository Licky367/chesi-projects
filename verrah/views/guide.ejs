<% if (
    user &&
    user.role === "staff"
) { %>

    <%- include("home-partials/staff/cards") %>


<% } else if (
    user &&
    user.role === "admin"
) { %>

    <%- include("home-partials/admin/cards") %>

<% } %>